# 선택 과제 — 모션그래픽 내 색으로 바꾸기

**선택 과제입니다.** 안 해도 진도에 지장이 없습니다. Day 7에서 만든 완성-1번·완성-2번·완성-3번 중 마음에 드는 스타일 하나를 골라, 그 스타일의 색만 내 채널 분위기에 맞게 바꿔 다시 만들어 봅니다. 코드는 건드리지 않고, 색 하나 바꾸는 것만으로 영상 느낌이 얼마나 달라지는지 확인하는 과제입니다.

## 1. 색 고르기

색은 이름으로 말해도 되고("남색", "베이지", "짙은 초록") 색 코드를 알면 그것도 됩니다("#1a2b3c"). 정답은 없지만, 채널 섬네일이나 로고에 이미 쓰는 색과 맞추면 통일감이 생깁니다.

지금 색이 궁금하면 Orca 왼쪽 파일 목록에서 `a-motion` 폴더 안 `색상설정.json`을 클릭해 열어 봅니다. 각 줄에 "설명"이 같이 적혀 있어 어느 값이 무엇인지 알 수 있습니다.

**이렇게 되면 성공:** 바꾸고 싶은 색을 한 문장으로 말할 수 있습니다. (예: "1번 스타일 배경을 짙은 남색으로")

## 2. Orca에 색 바꾸기를 시킨다

바꾸고 싶은 스타일의 색만 말합니다. 아래 문장을 그대로 쓰거나 색만 바꿔 씁니다.

```text
1번 스타일 배경을 짙은 남색으로, 강조색은 주황으로 바꿔 줘
```

```text
2번 스타일 배경을 밝은 베이지로, 강조색을 진한 초록으로 바꿔 줘
```

```text
2번 스타일 형광펜 색만 하늘색으로 바꿔 줘
```

```text
3번 스타일 배경 도형 색을 보라색으로 바꿔 줘
```

에이전트가 `색상설정.json` 안의 해당 값만 고쳐 씁니다.

**이렇게 되면 성공:** `색상설정.json`을 열어 보면 방금 말한 값으로 바뀌어 있습니다.

## 3. 새 색으로 다시 만든다

Day 7의 스타일 만들기와 똑같이 스타일 번호만 말합니다.

```text
2번 스타일로 만들어 줘
```

**주의:** `완성` 폴더의 파일 이름은 스타일 번호로 고정돼 있어(`완성-2번.mp4`), 다시 만들면 **원래 있던 파일을 덮어씁니다.** 색을 바꾸기 전 영상과 나란히 비교해 보고 싶다면, 다시 만들기 전에 아래처럼 먼저 사본을 만들어 두라고 시킵니다.

```text
완성-2번.mp4를 완성-2번-원래색.mp4라는 이름으로 복사해 줘
```

**이렇게 되면 성공:** `완성` 폴더에 새 색 영상(`완성-2번.mp4`)이 재생되고, 색이 바뀌어 보입니다.

## 4. 되돌리는 법

색이 마음에 안 들면 언제든 원래 색으로 되돌릴 수 있습니다. Orca에 "색상설정.json을 색상설정_원본.json 내용 그대로 되돌려 줘"라고 시키면 됩니다(방금 바꾼 것만 기억하고 있으면 "방금 바꾼 색을 원래대로 되돌려 줘"도 됩니다). 되돌린 뒤에는 3번과 같은 방법으로 다시 만들어야 화면에 반영됩니다.

**이렇게 되면 된 겁니다:** `색상설정.json`을 열어 보면 값이 `색상설정_원본.json`과 같습니다.

## 5. 색 고를 때 참고할 점

| 확인할 것 | 왜 |
|---|---|
| 배경색과 글자색의 밝기 차이가 충분한가 | 두 색이 밝기가 비슷하면(예: 회색 배경에 밝은 회색 글자) 폰 화면에서 글자가 흐릿하게 보이거나 아예 안 보입니다. |
| 강조색은 한 가지로 통일했는가 | 스타일 하나에 강조색을 여러 개 쓰면 어디가 진짜 핵심인지 눈에 안 들어옵니다. 2번 스타일의 형광펜색·강조색도 같은 계열로 맞추는 편이 낫습니다. |
| 배경색과 강조색이 너무 비슷하지 않은가 | 강조색이 배경에 묻히면 숫자·핵심 단어가 커도 안 띕니다. |

## 이렇게 되면 된 겁니다

바꾼 스타일을 다시 만들었을 때 `완성` 폴더의 영상이 재생되고, 배경·글자·강조 중 최소 하나가 확실히 달라진 색으로 보입니다. 글자는 여전히 또렷하게 읽힙니다.

## 오늘 제출

```text
[선택 과제 · 내 색으로 바꾸기]
바꾼 스타일: (1/2/3번)
바꾼 색: (예: 배경 남색 → 강조 주황)
이 색을 고른 이유 한 줄:
```

## 막히면 여기

| 증상 | 확인할 것 |
|---|---|
| 색을 바꿨는데 다시 만든 영상이 그대로임 | 3번 순서(다시 만들어 줘)를 빼먹지 않았는지 확인합니다. 색상설정.json만 고치는 것으로는 영상이 새로 만들어지지 않습니다. |
| "○○ 값이 색이 아닙니다" 안내가 뜸 | 색 이름을 Orca가 코드로 바꾸지 못한 경우입니다. 그 색을 `#rrggbb` 코드로 바꿔 다시 넣어 달라고 한 번 더 시킵니다. |
| "색상설정.json을 읽지 못해 기본 색으로 만들었습니다" 안내가 뜸 | `색상설정.json`의 파일이 망가진 것입니다. 위 "4. 되돌리는 법"의 색상설정.json 전체를 되돌리는 명령을 쓴 뒤 원하는 색을 다시 말합니다. |
| 글자가 배경에 묻혀 안 보임 | 위 "색 고를 때 참고할 점" 표를 보고 배경과 글자의 밝기 차이를 크게 벌리도록 다시 시킵니다. |
| 원래 색으로 못 돌아가겠음 | 값을 눈으로 읽어 옮겨 적을 필요 없습니다. 위 "4. 되돌리는 법"의 명령을 그대로 씁니다. |
| 완성 영상을 재생 중이라 다시 만들기가 실패함 | 영상을 열어 둔 재생 창을 닫고 다시 시킵니다. 파일이 재생 중이면 새로 쓸 수 없습니다. |
