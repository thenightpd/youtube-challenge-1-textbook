# Day 7 — 모션그래픽 ② 세 스타일 만들고 하나 고르기

여덟 단계 중 오늘: 제작(스타일 3종 만들어 보기) → 최종 검수(하나 고르기). 오늘은 도구에 익숙해지는 연습까지입니다 — **업로드는 하지 않습니다.** 첫 업로드는 이미 Day 5에서 일본어 채널에 했습니다.

> **오늘의 합격 기준:** 세 스타일을 모두 만들어 봤고, 폰으로 확인해 그중 하나를 골랐으며, 고른 이유를 한 줄로 말할 수 있습니다.

카드를 움직이는 건 키트가 자동으로 합니다. 앞서 만든 대본과 목소리를 넣으면 스타일에 맞춰 자동으로 움직이는 영상을 만들어 줍니다. 화면을 직접 그리거나 시간을 맞추는 작업은 없습니다 — Orca에 스타일 번호만 말하면 됩니다.

## 1. 세 가지 스타일

| 번호 | 이름 | 어떤 그림인가 |
|---|---|---|
| 1번 | 카드형 | 어두운 배경에 문장이 카드로 올라오고, 문장 속 숫자(26도, 만 오천 원)가 크게 뜹니다. 숫자가 없는 문장은 핵심 단어가 크게 뜹니다. |
| 2번 | 화이트보드 손그림 | 흰 종이에 문장이 한 줄씩 손으로 쓰이듯 쌓이고, 핵심 단어에 밑줄과 형광펜 표시가 그려집니다. |
| 3번 | 키네틱 타이포 | 어두운 배경에 아주 큰 글자가 말하는 박자에 맞춰 어절 단위로 하나씩 튀어나옵니다. |

## 2. 세 가지 다 만들어 보기

Orca 채팅에 한 번에 하나씩, 끝날 때까지 기다리며 시킵니다.

```text
1번 스타일로 만들어 줘
```

렌더(이렇게 영상 파일을 만들어 내는 것)에 1~5분 정도 걸립니다(컴퓨터 성능에 따라 다릅니다). 창을 닫지 않고 기다립니다.

**이렇게 되면 성공:** `a-motion`의 `완성` 폴더 안에 `완성-1번.mp4`가 생기고, 재생하면 목소리가 나오면서 카드가 움직입니다.

같은 식으로 2번, 3번도 만듭니다.

```text
2번 스타일로 만들어 줘
```

```text
3번 스타일로 만들어 줘
```

**이렇게 되면 성공:** `완성` 폴더 안에 `완성-1번.mp4`·`완성-2번.mp4`·`완성-3번.mp4` 세 개가 다 있습니다.

## 3. 폰으로 보고 하나 고르기

Orca 왼쪽 파일 목록에서 `a-motion` 폴더의 `완성` 폴더를 열어, 그 안의 세 파일을 폰으로 보내 세로로 재생합니다. PC 카카오톡을 켜고 그 파일들을 채팅 목록 맨 위 **나에게 보내기** 방으로 끌어다 놓으면, 폰 카카오톡의 같은 방에서 받을 수 있습니다.

- [ ] 첫 2초 안에 눈길을 끄는 글자가 보입니다.
- [ ] 소리를 꺼도 무슨 내용인지 알 수 있습니다.
- [ ] 가장 작은 글자도 확대 없이 읽힙니다.
- [ ] 화면 위아래(버튼 영역)에 글자가 가려지지 않습니다.

세 개 중 가장 잘 읽히고 잘 와닿는 것 하나를 고릅니다. 나머지 둘은 지우지 않아도 됩니다.

**이렇게 되면 성공:** 셋 중 가장 마음에 드는 것 하나를 정했고, 이유를 한 줄로 말할 수 있습니다(예: "숫자가 커서 소리 없이도 이해됨").

오늘은 여기까지입니다. 세 스타일을 파일로 만들어 보고 하나를 고르는 것이 모션그래픽의 목표이고, 유튜브 업로드는 하지 않습니다.

다음: Day 8(시니어 롱폼 1편 대본). 고른 스타일을 내 채널 색으로 바꿔 보고 싶으면 사이드바의 「선택 과제 · 내 색으로 바꾸기」로.

## 오늘 제출

```text
[Day 7]
고른 스타일: (1/2/3번) — 이유:
만든 것: 대본·목소리 + 완성-1번·2번·3번.mp4 (파일로만, 업로드 없음)
여덟 단계 중 어디까지: 최종 검수까지 (모션그래픽 연습 완료)
막힌 점 한 줄:
```

## 막히면 여기

| 증상 | 확인할 것 |
|---|---|
| "대본은 O문장인데 자막에서는 P개 문장이 나왔습니다" 오류 | 스튜디오에서 문장 하나가 둘로 쪼개지거나 합쳐진 것입니다. 다시 받아도 똑같이 실패하니, 오류 메시지에 나온 대로 Orca에게 대본을 자막의 문장 나눔에 맞게 다시 정리해 달라고 시킨 뒤 다시 만듭니다. |
| "음성 폴더에 mp3와 srt가 없습니다" 오류 | 받은 파일들을 음성 폴더에 넣어 달라고 Orca에 다시 시킵니다. 다운로드가 끝났는지도 확인합니다. |
| 렌더링이 5분 넘게 끝나지 않음 | 컴퓨터 성능에 따라 오래 걸릴 수 있습니다. 창을 닫지 말고 좀 더 기다립니다. 10분이 넘으면 Orca에 지금 상태를 물어봅니다. |
| "영상 렌더링에 실패했습니다" 오류 | 백신·회사망이 렌더용 크롬 다운로드를 막는 경우가 많습니다. 한 번 더 만들어 달라고 시키거나 집 네트워크에서 시도합니다. |
| 완성 영상을 재생 중이라 다시 만들기가 실패함 | 영상을 열어 둔 재생 창을 닫고 다시 시킵니다. 파일이 재생 중이면 새로 쓸 수 없습니다. |

<details>
<summary>이런 것들을 만들어 보세요 (펼치기, 선택)</summary>

오늘 고른 스타일 말고 다른 스타일도 같은 방법으로 만들어 볼 수 있습니다. 아래는 다음에 시도해 볼 만한 주제와 어울리는 스타일입니다.

| 주제 | 어울리는 스타일 | 왜 |
|---|---|---|
| 스마트폰 요금제 바꾸기 전후 비교 | 1번 카드형 | 숫자 비교(이전 요금 → 변경 요금)가 많은 주제라 큰 숫자 연출이 잘 맞습니다. |
| 배달 앱으로 배달비 아끼는 법 | 1번 카드형 | 할인율·배달비 숫자가 여러 번 나와 카드형의 숫자 강조가 잘 맞습니다. |
| 손글씨로 하루 계획 세우기 | 2번 화이트보드 | 손으로 쓰는 내용을 다루는 주제라 손그림 느낌과 잘 어울립니다. |
| 독서 습관 만드는 법 | 2번 화이트보드 | 차분한 톤의 주제라 손글씨가 쌓이는 흐름이 잘 맞습니다. |
| 아침 루틴 5단계 | 3번 키네틱 | 짧고 리듬감 있는 문장이 이어지는 주제라 어절 단위 등장이 잘 맞습니다. |
| 운동 전 스트레칭 순서 | 3번 키네틱 | 구호처럼 짧게 끊어지는 문장이 많아 큰 글자 연출이 잘 맞습니다. |

</details>
