# Day 1 — 준비

오늘은 영상을 만들지 않습니다. 계정을 만들고, 설치 파일 하나로 필요한 프로그램을 전부 깔고, Orca(에이전트 채팅과 파일 작업을 한 창에서 하는 무료 프로그램)를 열어 로그인까지 합니다. 순서대로 따라오면 1~2시간 안팎이면 끝납니다(설치 자체는 10~20분, 나머지는 대기 시간).

> **오늘 끝나면:** 코덱스나 클로드 중 하나와 타입캐스트에 가입돼 있고, 유튜브 채널이 2개 있고, `C:\yt1` 폴더 아래 프로그램·키트가 다 깔려 있고, Orca에서 로그인해 "안녕"이라고 쳐서 답을 받았습니다.

Windows 11 기준으로 씁니다. Windows 10도 같습니다.

---

## 1. 계정

| 무엇 | 어디서 | 이렇게 |
|---|---|---|
| 코덱스(ChatGPT) 또는 클로드 유료 | `chatgpt.com` / `claude.ai` | 둘 중 **하나만**. **코덱스(ChatGPT Plus)를 권합니다.** |
| 타입캐스트 | `typecast.ai` | 구글 계정으로 가입만 합니다. 지금은 결제하지 않아도 됩니다. 다만 시니어 롱폼 트랙(Day 8)부터는 무료 크레딧(서비스를 쓸 수 있는 사용량 — 평생 3,000크레딧 ≈ 5분)이 모자라 **Basic(월 $5)** 결제가 필요합니다. 보이스 목록에서 마음에 드는 한국어 목소리 이름을 하나 적어 둡니다. |
| 유튜브 채널 2개 | `youtube.com` | 아래 순서대로 |

코덱스를 권하는 이유: 시니어 롱폼 트랙에서 이미지를 여러 장 만들어야 하는데, 클로드는 이미지를 만들지 못해 결국 ChatGPT 무료 계정을 따로 써야 하기 때문입니다. 이미 클로드 Pro를 쓰고 있으면 그대로 써도 됩니다. 설치 파일은 둘 다 깔아 두니, 나중에 Orca에서 결제한 쪽으로 로그인하면 됩니다.

유튜브 채널은 한국어 채널 하나, 일본어 채널 하나로 두 개를 만듭니다. 한 구글 계정 안에서 만들 수 있습니다. **이미 쓰던 채널이 있으면 그중 하나를 그대로 써도 됩니다** — 부족한 개수만 새로 만들면 됩니다.

1. `youtube.com`에 로그인하고 오른쪽 위 프로필 사진을 누릅니다.
2. **설정**으로 들어가서 왼쪽 「계정」을 누릅니다.
3. **채널 추가 또는 관리** → **채널 만들기**를 누르고 이름은 아무거나 넣습니다. 이름은 나중에 바꿉니다.
4. 같은 방법으로 하나 더 만듭니다.
5. 각 채널에서 **YouTube 스튜디오**(유튜브 오른쪽 위 프로필 사진 → YouTube 스튜디오) **→ 설정 → 채널 → 기능 자격요건**을 열어 전화번호 인증을 해 둡니다. 맞춤 썸네일(영상 목록에 보이는 표지 그림을 내가 고른 그림으로 넣는 기능)과 15분 넘는 영상은 이 인증이 있어야 올라갑니다.

**이렇게 되면 된 겁니다:** 프로필 사진 → **계정 전환**을 누르면 채널이 2개 보입니다.

---

## 2. 설치 파일 하나로 프로그램 깔기

앞으로 쓸 도구는 이겁니다.

필요한 프로그램도 같이 깔립니다. 무엇이 깔리는지는 몰라도 됩니다.

이 전부와 작업 폴더 7개, 트랙 키트(각 트랙에 필요한 파일을 미리 묶어 놓은 것)까지 **설치 파일 하나**가 자동으로 깝니다. 그밤스튜디오만 따로 받습니다(Day 2).

| 순서 | 이렇게 | 이렇게 되면 된 겁니다 |
|---|---|---|
| 1 | 단톡방 공지에서 `유튜브챌린지-설치.zip`(zip — 여러 파일을 하나로 압축한 파일, 약 2MB)을 받습니다. 보통 **다운로드** 폴더에 저장됩니다 — 그 파일을 마우스로 끌어 **바탕화면**으로 옮긴 뒤, 오른쪽 클릭 → **압축 풀기**로 **통째로 바탕화면에 풉니다.** zip 안에서 바로 실행하지 않습니다. | 바탕화면에 폴더가 하나 생기고, 그 안에 `설치하기.cmd`가 보입니다. |
| 2 | 그 폴더 안의 **`설치하기.cmd`** 를 더블클릭합니다. | 검은 창이 뜨고 파란 글씨로 설치 진행 상황이 찍힙니다. |
| 3 | 「이 앱이 디바이스를 변경하도록 허용하시겠어요?」가 뜨면 **예**를 누릅니다. 프로그램마다 한 번씩, 서너 번 뜹니다. | 다음 프로그램 설치로 넘어갑니다. |
| 4 | Orca를 받는 동안(약 180MB) 몇 분 멈춘 것처럼 보입니다. 그냥 둡니다. | 설치 창이 잠깐 지나가고 검은 창으로 돌아옵니다. |
| 5 | 중간에 파란 「Windows의 PC 보호」(SmartScreen) 창이 뜨면 **추가 정보 → 실행**을 누릅니다. | 설치가 계속됩니다. 서명 없는 프로그램에 흔히 뜨는 정상 경고입니다. |
| 6 | 끝까지 **창을 닫지 않고** 기다립니다. 10~20분 걸립니다. | 아래 3번처럼 마지막 안내문이 찍히고, 아무 키나 누르면 창이 닫힙니다. |

**이미 있는 건 안 건드립니다.** 이미 깔린 프로그램·이미 만든 파일은 그대로 두고, API 키(프로그램이 다른 서비스에 접속할 때 쓰는 인증 문자열)도 만들지 않습니다.
실패한 항목은 창에 빨간 글씨로 보여 주고 `C:\yt1\install-log.txt`에도 남습니다. 다시 실행하면 된 것은 건너뛰고 나머지만 이어서 합니다.

---

## 3. 끝나면 화면에 이렇게 찍힙니다

모두 성공하면 검은 창 맨 아래에 이렇게 뜹니다.

```text
설치가 끝났습니다.

다음 할 일:
  1. 바탕화면의 Orca 를 엽니다.
  2. 왼쪽 위 폴더 열기에서 C:\yt1 을 고릅니다.
  3. 에이전트를 내가 결제한 쪽(코덱스 또는 클로드)으로 고르고 로그인합니다.
  4. 교재 Day 1 의 마지막 항목부터 이어서 합니다.
```

설치 마지막 단계에서 `doctor.py`(설치 상태를 대신 검사해 주는 파일)가 자동으로 한 번 돕니다. 이렇게 나옵니다.

```text
== 유튜브 자동화 챌린지 · 설치 검사 ==

  ✓ Python 3.11.9
  ✓ ffmpeg: C:\Users\...\ffmpeg.exe
  ✓ ffmpeg 자막 필터(libass)
  ✓ Node.js v22.14.0
  ✓ 자막 폰트(맑은 고딕, Windows 기본)
  ✓ 폴더 C:\yt1\a-motion
  ...
  ✓ 키트 a-motion/tts.py
  △ 그밤스튜디오  → 단톡방 공지에서 받아 C:\yt1\gbam 에 풀기(교재 Day 2)

모두 통과. Day 2 로 갑니다.
```

- **✓** 는 됐다는 뜻입니다.
- **△** 는 아직 안 해도 되는 항목입니다. 다음 날에 채워집니다.
- **✗** 가 있으면 오른쪽에 적힌 대로 고치고, **`설치하기.cmd`를 다시 실행**합니다(이미 된 항목은 건너뜁니다). 그래도 안 되면 이 화면을 캡처해서 단톡방에 올립니다.

나중에 뭔가 안 될 때는 바탕화면 설치 폴더의 **`점검하기.cmd`**를 더블클릭하면 `doctor.py`만 다시 돌아 지금 상태를 보여 줍니다.

---

## 4. Orca 열기 — 로그인과 첫 대화

앞으로 「Orca에 시킨다」는 Orca 채팅창에 문장을 쳐 넣는 것입니다. 검은 창에 명령을 치는 일은 없습니다.

1. 바탕화면의 **Orca** 아이콘을 더블클릭합니다.
2. 왼쪽 위 **폴더 열기**를 눌러 `C:\yt1`을 고릅니다.
3. 에이전트를 고릅니다. **내가 결제한 쪽 하나만** 고르면 됩니다.

| 내가 결제한 것 | 고를 것 | 로그인 |
|---|---|---|
| ChatGPT Plus | 코덱스 | **Sign in with ChatGPT** |
| 클로드 Pro | 클로드 | **Sign in with Claude** |

4. 로그인은 계정 로그인입니다. API 키를 넣는 곳이 아닙니다.
5. 채팅창에 `안녕`이라고 쳐서 Enter. 답이 옵니다.

**이렇게 되면 된 겁니다:** Orca 채팅창에 답이 보이고, 왼쪽에 작업 폴더 안의 하위 폴더들이 보입니다. Orca를 열고 작업 폴더가 열려 있는지는 이제부터 매번 확인할 필요 없습니다 — 껐다 켜도 마지막에 연 폴더가 그대로 열립니다.

왼쪽에 보이는 폴더 이름은 이런 뜻입니다.

| 폴더 | 무엇 |
|---|---|
| `a-motion` | 모션그래픽 |
| `b-remix` | 일본 쇼츠 |
| `d-longform` | 시니어 롱폼 |
| `gbam` | 그밤스튜디오 |
| `downloads` | 받은 원본 |
| `receipts` | 출처 기록 |
| `template` | 양식 |

폴더 이름을 외울 필요는 없습니다. Orca에게 「1편 폴더에」처럼 말하면 됩니다.

---

## 오늘 제출

```text
[Day 1]
코덱스 / 클로드: (하나)
타입캐스트 보이스 이름:
채널 2개 이름:
설치 결과: 실패 0개 / 또는 실패 항목 이름
Orca에서 "안녕" 답 받음: 예 / 아니오
```

---

## 막히면 여기

| 화면에 이렇게 나오면 | 이유 | 이렇게 합니다 |
|---|---|---|
| 창이 중간에 멈추거나 닫힘 | 네트워크 끊김·백신 차단 등 | `설치하기.cmd`를 다시 더블클릭합니다. 이미 끝난 항목은 건너뛰고 나머지만 이어서 합니다. |
| 검은 창 끝에 `[실패]` 항목이 남음 | 그 프로그램만 winget으로 못 받았습니다 | 항목 옆에 적힌 안내(직접 받을 주소)대로 그 프로그램만 설치한 뒤 `설치하기.cmd`를 다시 실행합니다. |
| `WinGet 이 없습니다` | Windows 10 구버전이라 winget이 없습니다 | 뜬 페이지에서 그 프로그램을 직접 설치합니다. Microsoft Store에서 **앱 설치 관리자**를 설치하면 winget이 생기기도 합니다. 그래도 안 되면 맨 아래 「직접 설치」로. |
| Python 항목이 `Microsoft Store 별칭으로 잡혀 있다`고 나옴 | Windows가 가짜 python을 먼저 잡습니다 | 이 화면을 캡처해서 단톡방에 올립니다. |
| 백신이 설치 파일을 지움 / `npm install`이 오래 걸리거나 실패 | 백신·회사 네트워크가 다운로드·설치를 막는 경우가 많습니다 | 백신 예외 폴더에 설치 폴더와 `C:\yt1`을 넣고 다시 실행합니다. 안 되면 집 네트워크에서 다시 합니다. |
| 회사 네트워크에서 빨간 오류가 길게 나옴 | 방화벽·프록시가 막는 경우가 많습니다 | 집 네트워크에서 다시 실행하거나, 화면 전체를 캡처해서 단톡방에 올립니다. |
| 이 PC에 `ANTHROPIC_API_KEY`·`OPENAI_API_KEY`가 이미 설정돼 있다는 노란 글씨가 뜸 | 이전에 API 키로 개발하던 흔적입니다 | 이 화면을 캡처해서 단톡방에 올립니다. |

---

<details>
<summary>설치 파일이 끝내 안 될 때 — 직접 설치 (펼치기)</summary>

설치 파일 대신 하나씩 손으로 깔 때만 봅니다. Windows에 들어 있는 `winget` 설치 도구로 프로그램마다 명령 한 줄이면 됩니다. 중간에 「이 앱이 디바이스를 변경하도록 허용하시겠어요?」가 뜨면 **예**를 누릅니다.

터미널(PowerShell) 여는 법: 키보드의 **Windows 키**를 누르고 `powershell`이라고 친 뒤 **Windows PowerShell**이 검색되면 **Enter**.

**① Chrome**

```powershell
winget install --id Google.Chrome -e --accept-package-agreements --accept-source-agreements
```

**② FFmpeg**

```powershell
winget install --id Gyan.FFmpeg -e --accept-package-agreements --accept-source-agreements
```

**③ Python 3.11**

```powershell
winget install --id Python.Python.3.11 -e --accept-package-agreements --accept-source-agreements
```

**④ Node.js**

```powershell
winget install --id OpenJS.NodeJS.LTS -e --accept-package-agreements --accept-source-agreements
```

네 개가 다 끝났으면 **PowerShell 창을 닫고 새로 엽니다.** 새로 깐 프로그램은 새 창에서만 잡힙니다.

**작업 폴더 만들기**

```powershell
New-Item -ItemType Directory -Force -Path C:\yt1\a-motion, C:\yt1\b-remix, C:\yt1\d-longform, C:\yt1\gbam, C:\yt1\downloads, C:\yt1\receipts, C:\yt1\template | Out-Null
```

**잘 깔렸는지 확인** — 새 창에서 하나씩 넣어 봅니다.

```powershell
ffmpeg -version
```

첫 줄에 `ffmpeg version 7.x`처럼 숫자가 나오면 됩니다.

```powershell
python --version
```

`Python 3.11.x`가 나오면 됩니다.

```powershell
node --version
```

`v22.x.x`처럼 나오면 됩니다.

**winget이 없거나 끝내 안 될 때 — 직접 받아 설치**

| 프로그램 | 받는 곳 | 설치할 때 |
|---|---|---|
| Chrome | `google.com/chrome` | 그냥 설치 |
| FFmpeg | `gyan.dev/ffmpeg/builds` → **release essentials**가 아니라 **release full** zip | `C:\ffmpeg`에 풀고, 설정 → 시스템 → 정보 → 고급 시스템 설정 → 환경 변수 → `Path`에 `C:\ffmpeg\bin` 추가 |
| Python 3.11 | `python.org/downloads` | 설치 첫 화면에서 **Add python.exe to PATH** 체크를 꼭 켭니다 |
| Node.js | `nodejs.org` → LTS | 그냥 설치 |
| Orca | `github.com/stablyai/orca/releases` → `orca-windows-setup.exe` | 그냥 설치 |
| 코덱스 CLI | (Node.js 설치 뒤) | PowerShell에서 `npm install -g @openai/codex` |
| 클로드 코드 | (Node.js 설치 뒤) | PowerShell에서 `npm install -g @anthropic-ai/claude-code` |

이렇게 깐 뒤에는 그밤스튜디오·트랙 키트(`a-motion-kit.zip` 등)를 단톡방 공지에서 따로 받아 각 폴더에 풀어야 합니다. 위치는 참고 탭 「도구·비용·설치 순서」의 키트 표를 봅니다.

</details>
