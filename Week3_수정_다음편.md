# 3주차 — 수정과 다음 편 틀

새 영상을 만들지 않아도 됩니다. 만들고 싶으면 일본 쇼츠 또는 시니어 롱폼으로 한 편 더.

> **3주차의 합격 기준:** ① 두 채널 모두 첫 편이 **공개**로 전환됐다 ② 양식 폴더 안에 다음 편에 그대로 쓰는 파일 4종이 채워져 있다 ③ 다음 편 소재 하나가 소재 기록표에 「제작 중」으로 올라가 있다.

## Day 15~17 — 첫 편 고치기

운영안 확인표의 「고칠 것」을 하나씩. 순서는 **소리 → 자막 → 첫 15초 → 썸네일·제목**.

| 고칠 것 | 어디서 | 다시 만드는 범위 |
|---|---|---|
| 목소리 톤 | 타입캐스트에서 보이스 바꿔 다시 다운로드 → Orca에 "목소리 만들어 줘" | 음성만 다시, 영상은 재조립 |
| 자막 위치·크기 | 자막 위치·크기는 이번 기수에서는 바꾸지 않습니다. | — |
| 첫 15초가 늘어짐 | 대본 첫 3문장 | 대본을 고쳐 **전체**를 다시 다운로드 → "목소리 만들어 줘" → "조립해 줘" |
| 썸네일 글자가 폰에서 안 읽힘 | ChatGPT 재생성 | 파일 교체만 |
| 출처 표기 누락 | `upload.txt` | 설명란 수정만 |

고친 편을 단톡방에 올리고 피드백을 받은 뒤 **공개**로 전환합니다. 공개 시각은 09~19시 사이로 예약합니다(새벽 발행은 초기 노출이 죽습니다 — 실측).

## Day 18~19 — 다음 편 틀 4종

양식 폴더에 이미 들어 있는 네 가지를 다음 편 값으로 채웁니다. 다음 편은 이 폴더를 복사하는 데서 시작합니다.

### ① 소재 기록표 `sources.md`
일본 쇼츠는 그밤스튜디오 소재함이 곧 기록표입니다. 나머지는 아래 표.

```text
| 날짜 | 제목 후보 | 출처 링크 | 채널(공식/개인) | 사용 조건·위험 | 쓸 구간 | 상태 | 메모 |
```
상태: 후보 / 검토 중 / 제작 중 / 발행 / 보류 / 사용 완료

### ② 대본 양식 `script-template.md`
세 트랙의 대본 구조를 빈칸으로 남겨 다음 편에 채워 씁니다.

- 모션그래픽: 훅(질문이나 놀라운 사실) → 배경 한 줄 → 방법 여러 가지 → 마무리(행동 권유) (6문장 안팎, `대본.txt`)
- 일본 쇼츠: 훅 → 질문 1 → 답 → 질문 2 → 답 → 결론 (30~50초)
- 시니어 롱폼: 로그라인 → 인물 3 → 반전① ② → 9비트(2편부터 15비트) → 비트별 대본 (9비트·15비트 모두 3,000자 안팎 = 8분 30초)

Orca에 시키는 **요청문**도 같은 파일에 붙여 둡니다.

### ③ TTS 설정 `tts.txt`
```text
엔진: 타입캐스트 스튜디오
보이스: (이름)
속도: 기본 (속도 조절은 Plus 이상)
문장 최대 글자: 30
다운로드: 전체 · 고음질 · mp3 · 한 파일로 합치기 + SRT
그 다음: Orca에 "목소리 만들어 줘"라고 시킨다
```

### ④ 채널 설정 채우기
양식 폴더에 이미 있는 파일을 내 채널 값으로 채웁니다.

```text
양식 폴더 파일들을 내 채널 설정으로 채워 줘
```

- 모션그래픽 카드: 스타일 번호(1/2/3) + `색상설정.json`(내 색으로 고친 것)
- 일본 쇼츠: 작업실 「기본 편집본」 설정 + 정지 카드 문구(일본어 문장)
- 시니어 롱폼: 시리즈 설정 + 컷 표 예시 + 인트로 영상(선택 과제를 했다면)

**이렇게 되면 성공:** 폴더를 복사해 파일명만 바꾸면, 다음 편의 여덟 단계 중 **소재 탐색과 각색만** 사람이 하면 되는 상태입니다.

## Day 20~21 — 다음 편 시작과 마무리

1. 소재 기록표에서 다음 편 소재를 하나 「제작 중」으로.
2. 여덟 단계 중 **각색까지** 해 둡니다(대본 초안). 제작은 챌린지 뒤에.
3. 마지막 제출:

```text
[3주차 · 마무리]
채널별 공개 편 링크 (한국어 · 일본어):
template 폴더 파일 4종: (있음/없음)
다음 편 소재와 상태:
가장 오래 걸린 단계와 이유:
챌린지 뒤 30일 계획 한 줄:
```

## 챌린지가 끝난 뒤

- 단톡방 피드백은 종료일까지. 그 뒤에는 방이 자료 열람용으로 남습니다.
- 작업 폴더는 지우지 않습니다. 양식과 출처 기록이 자산입니다.
- 도구 비용은 **성적이 나온 뒤** 늘립니다. 타입캐스트 Basic(월 $5)은 무료 크레딧(평생 3,000크레딧 ≈ 5분)이 모자라는 시니어 롱폼에서 먼저, 이미지·영상 생성 유료 구독은 주 1편 이상을 두 달 이상 지켰을 때.
