# Day 9 — 시니어 롱폼 1편 이미지 컷

여덟 단계 중 오늘: 씬·프롬프트 기획 → 제작(재료 중 이미지). 이미지는 ChatGPT로 만듭니다.

> **오늘의 합격 기준:** 이미지 폴더에 가로로 넓은 사진 17~20장이 있습니다(비트당 1장 + 긴 비트에 서브컷).

## 1. 이미지 프롬프트 표

비트 하나에 그림을 여러 장 씁니다. 한 비트 안의 두 번째·세 번째 그림을 **서브컷**이라 하고 01b, 01c처럼 이름 붙입니다. 오늘은 **비트당 1장 + 긴 비트(3·5·7·8)에 서브컷 = 17~20장**을 만듭니다. 8분 30초를 이 장수로 채우면 한 장이 25~30초씩 보입니다 — 시니어 시청자는 화면이 천천히 바뀌는 쪽을 편하게 봅니다. 못 채운 서브컷은 다음 날 앞부분에서 채웁니다.

모든 프롬프트 끝에 **공통 스타일 접미사**(모든 프롬프트 끝에 똑같이 붙이는 마무리 문구 — 채널 전체의 그림체를 통일해 줍니다)를 붙입니다. 샘플 채널이 쓴 것:

```text
Korean folk painting style (minhwa), soft watercolor texture, warm muted palette, Joseon dynasty setting, cinematic soft lighting, gentle brush strokes, no gore, no jump-scare imagery, respectful and warm mood
```

**한 채널 안에서는 접미사를 바꾸지 않습니다.**

Orca 채팅에 시켜 1편 이미지 프롬프트 표를 바로 만들게 합니다.

```text
1편 대본의 9비트 대본으로 1편 이미지 프롬프트 표를 만들어 줘.
비트마다 1장씩 배정하고, 비트 3·5·7·8에 서브컷(01b, 01c 등)을 하나씩 더해(총 17~20장). 각 프롬프트는 영어, 장면·인물·구도·감정을 한 문장씩, 끝에 이 접미사를 그대로 붙여: (접미사). 인물이 나오면 1편 대본의 "인물 외형" 문단을 앞에 붙여.
표 열: 파일명 | 비트 | 프롬프트
```

**이렇게 되면 성공:** 표에 12~15줄, 모든 줄이 같은 접미사로 끝납니다.

## 2. 이미지 생성 — ChatGPT, 16:9(가로 화면 비율)

이미지 생성은 Orca의 에이전트가 하지 못합니다. ChatGPT 웹·앱에서 만들고, 파일 정리만 Orca에 시킵니다.

클로드 Pro를 결제한 사람도 클로드는 이미지를 만들지 못합니다. ChatGPT 무료 계정으로 만듭니다.

1. ChatGPT 웹·앱에서 이미지 프롬프트 표의 프롬프트를 한 줄씩 넣습니다. 앞에 `16:9 가로 이미지로 생성:` 을 붙입니다(기본이 정사각형인 경우가 있습니다).
2. 생성된 이미지를 다운로드 폴더에 받습니다.
3. 다운로드가 끝나면 Orca 채팅에 시켜 옮깁니다.

```text
다운로드 폴더의 새 이미지들을 1편 이미지 프롬프트 표의 파일명 열에 맞춰 이름을 바꾸고 1편 그림 폴더로 옮겨 줘.
```

4. 얼굴이 다른 사람처럼 나오면 그 컷만 ChatGPT에서 "같은 인물, 이 외형 그대로: (외형 문단)"을 붙여 다시 만들고 같은 방식으로 옮깁니다.
5. 17~20장. 한 장에 30~60초. 40분~1시간 잡습니다.

**이렇게 되면 성공:** `images\` 안에 번호 순으로 정렬했을 때 이야기가 보이고, 고정 인물이 모든 컷에서 같은 사람입니다.

<div class="win-note"><b>이미지 생성 한도에 걸리면</b> — ChatGPT 플랜의 시간당 이미지 한도가 있습니다. 서브컷부터 빼고 비트당 1장(9장)만 먼저 만든 뒤, 한도가 풀리면 남은 서브컷을 다음 날 앞부분에서 채웁니다. 9장이어도 다음 날 조립은 됩니다.</div>

다음: Day 10(목소리와 시각표).

## 오늘 제출

```text
[Day 9]
만든 것: images __장
여덟 단계 중 어디까지: 제작(재료 중 이미지)까지
막힌 점 한 줄:
가장 마음에 드는 컷 1장 첨부:
```

## 막히면 여기

| 증상 | 확인할 것 |
|---|---|
| ChatGPT가 16:9로 안 만들어 줌 | "가로 1792×1024" 또는 "wide 16:9 aspect ratio"를 프롬프트 맨 앞에. 그래도 정사각이면 다음 날 조립에서 여백 없이 잘라 씁니다(위아래가 잘림). |
| 인물이 컷마다 바뀜 | 외형 문단을 프롬프트 **앞**에 두고, "same character as before"를 붙입니다. 얼굴 클로즈업 컷을 줄이고 중경 컷을 늘리면 덜 티 납니다. |
| Orca가 그림 순서를 모름 | 한 장 만들 때마다 바로 `이 그림은 03번이야, 1편 그림 폴더에 넣어 줘`라고 시킵니다. |
